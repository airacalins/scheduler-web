export enum AUTHENTICATION_FORM_TYPE {
  LOGIN,
  REGISTER,
}

export enum FORM_TYPE {
  TEXT = "text",
  EMAIL = "email",
  PASSWORD = "password",
  RADIO = "radio",
}

export enum ROUTE_NAME {
  LOGIN = "/login",
  REGISTER = "/register",
}

export enum VARIANT {
  PRIMARY = "primary",
  DARK = "dark",
}